<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="@dimen/contactMarginBottom"
    android:gravity="center_vertical"
    android:orientation="horizontal">

    <TextView
        android:id="@+id/contactName"
        android:layout_width="@dimen/contactNameWidth"
        android:layout_height="wrap_content"
        android:layout_marginLeft="@dimen/contactNameMarginLeft"
        android:layout_weight="1"
        android:textStyle="bold" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/callFab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginRight="@dimen/callIconMarginRight"
        android:clickable="true"
        app:backgroundTint="@android:color/darker_gray"
        app:srcCompat="@drawable/ic_baseline_call_24" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/messageFab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginRight="@dimen/messageIconMarginRight"
        android:clickable="true"
        app:backgroundTint="@android:color/darker_gray"
        app:srcCompat="@drawable/ic_baseline_message_24" />

</LinearLayout>